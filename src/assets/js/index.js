var activeFlag = 'is-active';

$(function () {
  $( '.js-youtube-slider' ).sliderPro({
    width: 320,
    arrows: true,
    buttons: false,
    slideDistance:20,
    visibleSize: '100%',
    imageScaleMode : 'contain',
  });
});